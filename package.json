{
  "name": "arxiv-dark-mode",
  "version": "0.6.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/sync-legacy-artifact.mjs",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
